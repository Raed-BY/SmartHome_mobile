name: mobile_app
description: SmartHome App
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  mqtt_client: ^10.11.10
  speech_to_text: ^7.3.0
  local_auth: ^2.1.9
  flutter_secure_storage: ^8.0.0
  flutter_ringtone_player: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/icons/